# Ship Report - release-3.2.1-ship (consolidated)

- **Delivered**: 2026-09-07, push `dbf4d38..3b39825 main -> main` (fast-forward), https://github.com/IIXINGCHEN/ai-skills-pro
- **Verdict**: DONE (completion-gate.md, issued before commit and push)
- **Delivery result**: PUSHED

## Findings matrix vs final states

| Finding | Severity | Final State |
|---|---|---|
| F1 version drift (plugin/marketplace/registry at 3.1.0) | Critical | Resolved |
| F2 RELEASE.md stale version line | Warning | Resolved |
| F3 sync-version.mjs regex root cause | Warning | Resolved (+ regression test) |
| F4 RELEASE.md stale counts (40 skills / 51 tests) | Warning | Resolved (42 / 62->64 documented) |
| F5 QUALITY_REPORT.md 3.1.0 snapshot | Warning | Resolved (regenerated from fresh gate outputs) |
| F6 ARCHITECTURE.md stale count | Warning | Resolved |
| F7 no-op replace (dead code) | Suggestion | Resolved; re-root-caused as W-2 |
| F8 link-skills.sh comment typo | Suggestion | Resolved |
| F9 .npmignore dead config | Suggestion | Deferred (zero functional impact; next housekeeping change) |
| W-2 unspaced empty flow sequences in manifests | Warning | Resolved (+ corpus-wide regression test; PyYAML misparse evidence) |

## Fixes applied

- Version alignment across 6 surfaces via project-blessed scripts (`sync-plugin-version.mjs`, `generate:manifests`) + minimal marketplace edit.
- `sync-version.mjs` regex corrected to `^Release version:` (root cause of recurring RELEASE.md drift).
- `generate-manifests.mjs` yamlKey spacing fix for inline values (empty flow sequences) + dead no-op removed.
- Release docs regenerated/corrected with freshly verified numbers only (ADR 0005).
- 2 regression tests added to `tests/repo.test.mjs` (suite 62 -> 64; both fail on the pre-fix tree).

## Validation history

- `npm run validate`: 3 runs green (42 skills, 0 errors, 0 warnings)
- `npm test`: 3 runs green (63/63, 64/64, 64/64)
- `npm run eval`: 3 runs green (train 34/34, holdout 17/17, blind 11/11; output contracts 20/20)
- `npm run release-check`: 3 runs green (0 errors; 2 accepted working-tree notices)
- Convergence: Pass 1 CHANGES REQUESTED -> repair -> Pass 2 APPROVED after W-2 repair -> Pass 3 clean APPROVED (p=3)

## Commit hashes

- `62391d542ba7678ea81b3d53694240842be852a7` - original root commit (pre-graft; tree identical)
- `3b398256d2018106166222558dde87e15cedf8ee` - delivered commit (parent `dbf4d38`, grafted via soft reset; fast-forward push under explicit PUSH authorization after stability re-check)

## Delivery result

PUSHED. Remote `refs/heads/main` verified `3b39825` post-push; local `main` tracking `origin/main`, in sync. Force push not used (hard-blocked policy; not needed - fast-forward).

## Residual risks (recorded, non-blocking)

- F9 deferred (cosmetic config debt).
- Mimosa callgraph-level deep scan has not run against this commit; security evidence is limited to the gauntlet record (secrets sweep, contamination scan, SSRF boundary checks, zero-dependency runtime). A full audit is recommended as follow-up.
- This report and the pipeline state file were finalized after the push; they exist locally and can ride the next authorized commit.

Reports in this folder: review-1/2/3.md, remediation-plan.md, validation-report.md, completion-gate.md, execution record, this ship report.

---

# Addendum: post-delivery corrective commit (3e92daf)

Remote verification of the delivered commit (`3b39825`, checked against api.github.com) exposed four defects the convergence gauntlet missed: the local snapshot was of mixed vintage - CHANGELOG/VERSION were post-`fb24f93` but four files predated it. The Stage 7 fetch comparison counted the 63-file delta but did not audit per-file diff direction against the remote lineage.

| # | Defect | Root cause | Fix |
|---|---|---|---|
| PD-1 | validate-skills.yml downgraded Node 22 -> "20", contradicting CHANGELOG 3.2.1 and diverging from release.yml | snapshot predated `fb24f93` | restored from `dbf4d38`; new test pins one common Node major across workflows |
| PD-2 | tests/read-json.test.mjs lost the mkdirSync that clean CI checkouts need (clean clone would fail the suite; `.scratch/` is untracked and now gitignored) | same | restored from `dbf4d38` |
| PD-3 | vis-reverse-ui/SKILL.md lost the SSRF defense boundary bullet the same CHANGELOG entry claims | same | restored from `dbf4d38`; manifests + registry regenerated for the new hash; new test pins the boundary |
| PD-4 | prior ship report specs/convergence-gauntlet-consolidation/reports/ship-2026-09-07T173000Z.md deleted | snapshot lacked it | restored from `dbf4d38` |

Also in this commit: two regression guards in tests/upstream-conventions.test.mjs (workflows pin one Node major; SSRF boundary present), and a workstation path redacted from this pipeline's execution record (zero-absolute-path policy; deviation from append-only doctrine noted here - original preserved in `3b39825` history). Registry note: `.scratch/.gitkeep` remains untracked by design; the restored mkdirSync is the mechanism that guarantees the directory on clean checkouts.

Evidence: all four gates green on the corrective tree (validate 0 errors, test 66/66, eval 34/34 + 17/17 + 11/11 + 20/20, release-check 0 errors). Process lesson recorded: when shipping a snapshot onto a live remote lineage, the gauntlet must diff every modified file's direction, not just count the delta; "snapshot newer" was assumed where it had to be verified.

